# Everything a run writes. Regenerated by running the example, never committed.
# The five text files are the complete set the tracker can write beside the HDF5.
*.h5
*.log
*.png
*.diag.txt
*.import.txt
*.ledger.txt
*.migration.txt
*.wake.txt
saturation_demo/output/
