! SASE with wakefields: the sase.nml run through a deliberately NARROW copper chamber
! (0.5 mm radius -- a tuned demonstration case; at this energy and wavelength a normal
! chamber's wake is small, and the point here is to SEE the physics, honestly labeled)
! plus the undulator gap wake and a rough surface. The per-slice energy loss is written
! to sase_wake.wake.txt; the mean-energy panel of plot_fel.py shows the wake-driven
! decline on top of the FEL's own energy extraction.
!
!   fel_track_test run.nml           (run from this directory)
!   python ../plot_fel.py sase_wake.stats.h5

&fel_params
  lat_file = "../aramis.bmad"
  global%out_root = "sase_wake"

  wake%on = T
  wake%radius = 0.5e-3         ! Narrow chamber: the tuned, visible case.
  wake%material = "CU"         ! Copper: conductivity + AC relaxation shortcuts.
  wake%gap = 0.5e-3            ! Undulator gap wake...
  wake%lgap = 0.015            ! ...one gap per period.
  wake%hrough = 100e-9         ! 100 nm surface roughness...
  wake%lrough = 100e-6         ! ...with 100 um correlation length.
/

&fel_beam_init


  beam_init%n_particle = 2048
  beam_init%bunch_charge = 2.881993782512e-13  ! 3 kA flat: I = Q*c/extent.
  beam_init%distribution_type(3) = "GRID"  ! Flat bunch (Bmad's uniform)...
  beam_init%grid(3)%x_min = -1.44e-8       ! ...spanning the full 96-slice window.
  beam_init%grid(3)%x_max =  1.44e-8
  beam_init%sig_pz = 8.804506566858e-5     ! delta_gamma = 1.0 m_e c^2.
  beam_init%a_norm_emit = 4e-7
  beam_init%b_norm_emit = 4e-7
  shotnoise = T
/

&fel_wavefront_init

  wavefront_init%lambda0 = 1e-10

  wavefront_init%seed_power = 0
  wavefront_init%grid_n_pts = 255
  wavefront_init%grid_half_width = 2e-4

  wavefront_init%window_length = 2.88e-8      ! 96 slices of spacing 3*lambda0, as sase/run.nml.
  wavefront_init%window_sample = 3
/
